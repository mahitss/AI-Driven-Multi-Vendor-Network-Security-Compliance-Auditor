#config-version=FG60D-5.6.0-build0000
# NetVigil Synthetic Fixture: Fortinet FortiOS Legacy Insecure Firewall
config system global
    set hostname "VULN-FGT-02"
    set admin-ssh-v1 enable
    set strong-crypto disable
end

config custom unknown-section
    set weird-parameter-val activate
end
