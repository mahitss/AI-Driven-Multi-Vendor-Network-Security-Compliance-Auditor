#config-version=FG60F-7.2.4-FW-build1396-230308:opmode=0:vdom=0:user=admin
# NetVigil Synthetic Fixture: Fortinet FortiOS Secure Firewall Baseline
config system global
    set hostname "SECURE-FGT-01"
    set timezone "80"
    set admintimeout 10
    set admin-ssh-v1 disable
    set admin-lockout-threshold 3
    set admin-lockout-duration 300
    set pre-login-banner enable
    set strong-crypto enable
end

config system interface
    edit "lan"
        set allowaccess ping https ssh
    next
end

config system ntp
    set ntpserver "10.100.5.1"
end

config log syslogd setting
    set status enable
    set server "10.100.20.50"
end

config firewall policy
    edit 1
        set name "DENY_ALL"
        set action deny
    next
end
